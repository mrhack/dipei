{# block header #}
<section class="top">
    <div class="top-color" ></div>
    <div class="top-con">
        <a href="/"><img class="logo" src="{{'image/logo.png'|url}}" alt="{{_e('乐陪')}}" title="{{_e('乐陪')}}"/></a>
        <div class="top-r clearfix">
            <div class="top-r-w">
                <button class="btn btn-pub">{{_e('发布一个鲜旅')}}</button>
            </div>
            <div class="top-r-sep"></div>
            <div class="top-r-w">
                <i class="i-icon i-user"></i>
                {{_e('注册帐户，已有帐户请登录')}}
            </div>
            <div class="top-r-sep"></div>
            <div class="top-r-w">
                <span class="msg-num">4</span>
                <i class="i-icon i-msg"></i>
            </div>
            <div class="top-r-sep"></div>
            <div class="top-r-w">
                {% set locals = var('LOCALS') %}
                <i class="i-country i-"></i>
                <ul class="dropdown-menu" c="lang" style="height: 200px;overflow-y: scroll;">
                    {% for key , local in locals %}
                        <li c="{{key}}"><a><i class="i-country i-{{key}}"></i>{{local}}</a></li>
                    {% endfor %}
                </ul>
            </div>
            <div class="top-r-sep"></div>
            <div class="top-r-w">
                {% set moneys = var('MONEYS') %}
                {{TRANSLATES[request('money' , 'cookie')]}}
                <ul class="dropdown-menu money-list" c="money" style="height: 200px;overflow-y: scroll;">
                    {% for money_tid , money in moneys %}
                    <li c="{{money_tid}}">
                        <a><span class="symbol">{{moneys[money_tid].symbol}}</span>{{TRANSLATES[money_tid]}}</a>
                    </li>
                    {% endfor %}
                </ul>
            </div>
            <div class="top-r-sep"></div>
        </div>
    </div>
</section>