* read me text file
* static for dipei application. The main job is make the client browser cache newwest. Cache the static file version, include javascript, stylesheet
* images and so on.

* dipei application use seajs to load js and other relay resources
* the static cgi is to produce the config of seajs. The main jobs just like follows:

* 1. renew the config of seajs
* 2. generate all static files version
* 3. compress the js and css file which was renewed.
* 4. combine the files
* 5. use less to manage all css files 
