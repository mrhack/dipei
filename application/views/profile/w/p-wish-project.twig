{% import "base/micro.twig" as Micro %}
<section class="p-location" >
    <div class="p-head">
        <p class="fs20 yahei">{{_e('我收藏的项目')}}</p>
    </div>
    <div class="p-body">
        <ul class="wish-posts">
            {% for pid in wish_projects %}
                {% include "widget/post-item.twig" with {post: PROJECTS[pid]} %}
            {% else %}
                <li class="c999">{{_e("暂无数据")}}</li>
            {% endfor %}
        </ul>
    </div>
</section>