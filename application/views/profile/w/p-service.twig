{% import "base/macro.twig" as Macro %}
<section class="p-service">
    <div class="p-head">
        <p class="fs20 yahei">{{_e('我提供的服务')}}</p>
        <p>{{_e('请先 <span>绑定你的支付宝账户</span> 才能提交服务，否则你将无法收到付款')}}  <i class="i-icon i-help"></i></p>
    </div>
    <div class="p-body">
        <table class="project-list">
            <tbody>
                {% for pid in USERS[UID].projects %}
                {% set p = PROJECTS[pid] %}
                <tr>
                    <td><span class="p-index">P{{loop.index}}</span></td>
                    <td><a class="mgr10" href="/profile/host/service/?pid={{p.id}}">{{p.title}}</a>{{Macro.projectstatus(p.status)}}</td>
                    <td style="width:100px;"><a href="/post/3/{{p.id}}/" target="_blank" class="mgr10">{{_e('预览')}}</a><a href="/profile/host/service/?pid={{p.id}}&op=update" class="mgr10">{{_e('编辑')}}</a><a data-a="p-remove" data-D="pid={{p.id}}" href="javascript:;">{{_e('移除')}}</a></td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        <a href="/profile/host/service/?op=new" class="btn btn-green">{{_e('继续创建')}}</a>
    </div>
</section>